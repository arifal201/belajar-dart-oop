import 'data/location.dart';

/**
 * belajar membuat abstract class yang dimana abstract class tidak dapat di initiate atau tidak dapat dibuat objectnya
 * biasanya abstract class dibuat untuk class parent yang tidak dapat di initiate atau hanya sebagai parent
 */

void main(){
  var city = City('Bandung');
  // var location = Location(); //error
}